<!DOCTYPE html>
<html>
    <head>
        <title>YelpCamp</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    </head>
    <body>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                    <div class="container-fluid">
                        <div class="navbar-header">
                          <a class="navbar-brand" href="/">YelpCamp</a>  
                        </div>
                        <div class="collapse navbar-collapse" >
                            <ul class="navbar-nav ml-auto nav-flex-icons">
                                <% if(!currentUser) { %>
                                <li class="nav-item active"><a class="nav-link waves-effect waves-light" href="/login">Login</a></li>
                                <li class="nav-item active"><a class="nav-link waves-effect waves-light" href="/register">Sign Up</a></li>
                               <% } else { %>   
                                <li class="nav-item active"><a class="nav-link waves-effect waves-light" href="#">Signed In As <%= currentUser.username %></a></li>
                                <li class="nav-item active"><a class="nav-link waves-effect waves-light" href="/logout">Logout</a></li>
                              <% } %>
                              </ul>
                        </div>
                    </div>
                </nav>
                <div class="container">
                    <% if(success && success.length > 0 ) {%>
                    <div class="alert alert-success" role="alert">
                        <%= success %>
                    </div>
                    <% } %>
                    <% if(error && error.length > 0 ) {%>
                    <div class="alert alert-danger" role="alert">
                        <%= error %>
                    </div>
                    <% } %>
                </div>

            